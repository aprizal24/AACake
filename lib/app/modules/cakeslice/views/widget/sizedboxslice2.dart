import 'package:flutter/material.dart';

class SizedBoxSlice2 extends StatelessWidget {
  const SizedBoxSlice2({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15.0);
  }
}
