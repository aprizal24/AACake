import 'package:flutter/material.dart';

class SizedBoxSlice extends StatelessWidget {
  const SizedBoxSlice({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15.0);
  }
}
