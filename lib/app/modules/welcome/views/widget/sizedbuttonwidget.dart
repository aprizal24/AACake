import 'package:flutter/material.dart';

class SizedButtonWidget extends StatelessWidget {
  const SizedButtonWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 20);
  }
}
