import 'package:flutter/material.dart';

class SizedBoxChif2 extends StatelessWidget {
  const SizedBoxChif2({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 7.0);
  }
}
