import 'package:flutter/material.dart';

class PaddingChif2 extends StatelessWidget {
  const PaddingChif2({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Container(
        color: const Color(0xFFEBEBEB),
        height: 1.0,
      ),
    );
  }
}
