import 'package:flutter/material.dart';

class SizedBoxChif extends StatelessWidget {
  const SizedBoxChif({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15.0);
  }
}
